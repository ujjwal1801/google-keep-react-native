import { StyleSheet } from "react-native"

const stylesFn = () => StyleSheet.create({
    container:{
        flexDirection: 'row'
    },
    textbox:{
        backgroundColor: '#fff',
        borderRadius: 10,
        width: '85%'
    },
    addBtn:{
        borderRadius: 10,
        backgroundColor: 'fff'
    }

})

export default stylesFn